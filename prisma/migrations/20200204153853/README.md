# Migration `20200204153853`

This migration has been generated by paul at 2/4/2020, 3:38:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Post" (
    "author" integer  NOT NULL ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "description" text  NOT NULL DEFAULT '',
    "id" SERIAL,
    "title" text  NOT NULL DEFAULT '',
    "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Category" (
    "id" SERIAL,
    "name" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."_CategoriesOfPost" (
    "A" integer  NOT NULL ,
    "B" integer  NOT NULL 
) 

CREATE UNIQUE INDEX "_CategoriesOfPost_AB_unique" ON "public"."_CategoriesOfPost"("A","B")

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("author") REFERENCES "public"."User"("id") ON DELETE RESTRICT

ALTER TABLE "public"."_CategoriesOfPost" ADD FOREIGN KEY ("A") REFERENCES "public"."Category"("id") ON DELETE CASCADE

ALTER TABLE "public"."_CategoriesOfPost" ADD FOREIGN KEY ("B") REFERENCES "public"."Post"("id") ON DELETE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200204131447-user..20200204153853
--- datamodel.dml
+++ datamodel.dml
@@ -7,14 +7,14 @@
 // Learn more about connection strings for your DB: https://pris.ly/d/connection-strings
 datasource db {
   provider = "postgresql"
   // other options are: "mysql" and "sqlite"
-  url = "***"
+  url      = "postgresql://postgres:vlzkcb12@localhost:5432/hello-prisma2?schema=public"
 }
 // Other examples for connection strings are:
-// SQLite: url = "***"
-// MySQL:  url = "***"
+// SQLite: url = "sqlite:./dev.db"
+// MySQL:  url = "mysql://johndoe:johndoe@localhost:3306/mydb"
 // You can also use environment variables to specify the connection string: https://pris.ly/d/prisma-schema#using-environment-variables
 // By adding the `generator` block, you specify that you want to generate Prisma's DB client.
 // The client is generated by runnning the `prisma generate` command and will be located in `node_modules/@prisma` and can be imported in your code as:
@@ -32,5 +32,22 @@
 model User {
   id    Int     @id @default(autoincrement())
   email String  @unique
   name  String?
+  posts Post[]  @relation(name: "PostsOfUser")
+}
+
+model Post {
+  id          Int         @id @default(autoincrement())
+  author      User        @relation(name: "PostsOfUser")
+  title       String
+  description String
+  categories  Category[]  @relation(name: "CategoriesOfPost")
+  createdAt   DateTime    @default(now())
+  updatedAt   DateTime    @updatedAt
+}
+
+model Category {
+  id    Int     @id @default(autoincrement())
+  name  String
+  posts Post[]  @relation(name: "CategoriesOfPost")
 }
```


